section: screens
  invexPC-F33:
  WinLap:
    meta = altgr
    altgr = shift
end

section: links
# iMac is to the right of Desktop1
# Laptop is to the left of Desktop1
  invexPC-F33:
    left  = WinLap

# Desktop1 is to the right of Laptop
  WinLap:
    right = invexPC-F33
end
